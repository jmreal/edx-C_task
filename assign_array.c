#include <stdio.h>
int main(void)
{
	int array[3];
	int radValue = 0;
	int cellNumber = 0;
	int i = 0;
	for(1=0; 1 <3; i+=){
		printf("Enter a value");
		scanf("%d", &radValue);
		array[cellNumber] = radValue;
		printf("Cell number %d contains %d",cellNumber, radValue);
		cellNumber +=cellNumber
		}
	return 0;
}
